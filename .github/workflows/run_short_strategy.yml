name: 24h Short Strategy Bot

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  run-strategy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Start Xray Proxy
      run: |
        wget https://github.com/XTLS/Xray-core/releases/download/v1.8.4/Xray-linux-64.zip
        unzip -o Xray-linux-64.zip
        chmod +x xray
        nohup ./xray -config config.json > /dev/null 2>&1 &
        sleep 5

    # --- 修改点：注入 Server酱 Key ---
    - name: Run Strategy Script
      env:
        SERVERCHAN_KEY: ${{ secrets.SERVER_JIANG_KEY }}  # 这里把 Secret 传给 Python
      run: python short_top10_gainers_3x_rotation.py

    - name: Save Data
      run: |
        git config --global user.name "StrategyBot"
        git config --global user.email "bot@github.com"
        
        # 1. 先把生成的文件添加到暂存区
        git add strategy_state.json strategy_history.csv equity_curve.csv
        
        # 2. 检查是否有变化
        if [[ -n $(git status -s) ]]; then
          # 3. 先提交到本地
          git commit -m "📊 Update Data & Equity Curve [$(date)]"
          
          # 4. 【核心修复】拉取远程最新代码并变基（解决冲突的关键）
          git pull --rebase origin main
          
          # 5. 推送
          git push
        else
          echo "No changes."
        fi
