name: 24h Short Strategy Bot

#on:
#  schedule:
#    - cron: '*/15 * * * *'
#  workflow_dispatch:

on:
  schedule:
    # 每小时的第0、15、30分钟各运行一次（例如 12:00, 12:20, 12:40）
    - cron: '0,15,30,45 * * * *'
  workflow_dispatch:

jobs:
  run-strategy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Start Xray Proxy
      run: |
        wget https://github.com/XTLS/Xray-core/releases/download/v1.8.4/Xray-linux-64.zip
        unzip -o Xray-linux-64.zip
        chmod +x xray
        nohup ./xray -config config.json > /dev/null 2>&1 &
        sleep 5

    # --- 修改点：注入 Server酱 Key ---
    - name: Run Strategy Script
      env:
        SERVERCHAN_KEY: SCT281228TBF1BQU3KUJ4vLRkykhzIE80e  # 这里把 Secret 传给 Python
      run: python short_top10_gainers_3x_rotation.py

    - name: Save Data
      run: |
        git config --global user.name "StrategyBot"
        git config --global user.email "bot@github.com"
        
        # 1. [核心修复] 开启自动暂存，如果遇到未提交的脏数据，先存起来再拉取
        git config --global rebase.autoStash true
        
        # 2. [改进] 使用 git add . 添加所有变更，防止有文件漏网导致“未暂存”错误
        git add .
        
        # 3. 检查是否有状态变化
        if [[ -n $(git status -s) ]]; then
          git commit -m "📊 Update Data & Equity Curve [$(date)]"
          
          # 4. 拉取并变基 (现在即使有脏数据，autoStash 也会处理)
          git pull --rebase origin main
          
          git push
        else
          echo "No changes."
        fi
